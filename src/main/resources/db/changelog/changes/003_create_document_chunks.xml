<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-create-document-chunks" author="bytehr">
        <createTable tableName="document_chunks">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="document_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_chunks_document"
                             references="documents(id)"
                             deleteCascade="true"/>
            </column>
            <column name="content" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="chunk_index" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="page_number" type="INTEGER"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <!-- Add vector column via raw SQL as Liquibase does not have native vector type support -->
        <sql>ALTER TABLE document_chunks ADD COLUMN IF NOT EXISTS embedding vector(768);</sql>
        <createIndex tableName="document_chunks" indexName="idx_chunks_document_id">
            <column name="document_id"/>
        </createIndex>
        <!-- IVFFlat index for approximate nearest neighbour search -->
        <sql>CREATE INDEX IF NOT EXISTS idx_chunks_embedding ON document_chunks
             USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);</sql>
    </changeSet>

</databaseChangeLog>
