Using the approved architecture:

Generate the complete Spring Boot project skeleton.

Requirements:

* Java 21
* Spring Boot 3
* Maven
* PostgreSQL
* pgvector

Create:

* pom.xml
* application.yml
* package structure
* configuration classes
* DTOs
* entities
* repositories
* services
* controllers

Generate empty implementations only.

No business logic.

The project must compile successfully after generation.
